@model Admin
@{
    List<User> users2 = ViewBag.users;
    List<Admin> adminler = ViewBag.adminler;
    List<String> users = ViewBag.userList;
    
    Layout="_Layout3";
}
<style>
    .deneme:hover{
        background-color: #bebebe;
  color: white;
    }
</style>
<h1>Adminler</h1>
        <hr>
<div class="row mb-2 ">
    <a href="/admin/addadmin" class="btn btn-primary mr-auto" style="margin-bottom: 20px; margin-left: 10px;">Admin Ekle</a>
</div>
<div class="row">
    <div class="col-md-12">
        
        
        @if (adminler.Count>0)
        {
            <table class="table table-bordered mt-3">
            <thead>
                <tr>
                    <td style="width: 50px;">Id</td>
                    <td style="width: 50px;">Isim</td>
                    <td style="width: 100px;">SoyIsim</td>
                    <td style="width: 100px;"></td>
                    
                </tr>
            </thead>
            <tbody>
                @foreach (var item in adminler)
                {
                    <tr class="deneme">
                            <td>@item.AdminID</td>                           
                            <td>@item.name</td>
                            <td>@item.surName</td>
                            <td>
                                <a href="#" class="btn btn-primary btn-sm mr-2">Guncelle</a>
                                <a href="#" class="btn btn-danger btn-sm mr-2">Sil</a>
                            </td>
                        </tr>
                }
                
            </tbody>
        </table>
        }
        <h1>Kullanicilar</h1>
        <hr>
        @* @if (users.Count>0)
        {
            <table class="table table-bordered mt-3">
                <thead>
                    <tr>
                        <td style="width: 100px;">Isim</td>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in users)
                {
                    <tr class="deneme">
                        <td style="">@item</td>                           
                        
                    </tr>
                }
                </tbody>
            </table>
        } *@
        @if (users2.Count>0)
        {
            <table class="table table-bordered mt-3">
                <thead>
                    <tr>
                    <td style="width: 100px;">Kullanici Adi</td>
                    <td style="width: 75px;">Isim</td>
                    <td style="width: 100px;">SoyIsim</td>
                    <td style="width: 100px;">Email</td>
                    </tr>
                </thead>
                <tbody>
                @foreach (User item in users2)
                {
                    <tr class="deneme">
                        <td style="">@item.UserName</td>
                        <td style="">@item.FirstName</td>
                        <td style="">@item.LastName</td>                           
                        <td style="">@item.Email</td>
                    </tr>
                }
                </tbody>
            </table>
        }
    </div>
</div>

